module.exports = results => {
	// this sort of thing might not even be necessary
	// if there actually is a common transform from the database
	// results to json, you would make that a helper function
	return results
}
